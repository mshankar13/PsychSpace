/**
 * Javascript for all pages /learn*
 */

$(document).ready(function () {
    var goal = $("#goal").val();
    if (goal != "")
        loadGoal(goal);
});

function loadGoal(goal) {
    var value = $("#value").val();
    var unit = $("#unit").val();


}
